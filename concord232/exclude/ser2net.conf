TRACEFILE:readlog:/tmp/concord-readlog
TRACEFILE:writelog:/tmp/concord-writelog
TRACEFILE:bothlog:/tmp/concord-bothlog

# Use "telnet" mode (not "raw") to enable RFC2217 Telnet option negotiation,
# which is required by pyserial's rfc2217:// URL handler.
# Port must match the rfc2217:// URL in config.ini / add-on options (default 5500).
5500:telnet:0:/dev/cu.usbserial-AH01JXFE:9600,tr=readlog,tw=writelog,tb=bothlog,hexdump
